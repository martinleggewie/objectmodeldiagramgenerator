package org.codemaker.objectmodeldiagramgenerator.domain.entities;

public enum OmgOrigin {
  outside,    // object exists outside the current scenario
  inside      // object exists inside the current scenario
}
